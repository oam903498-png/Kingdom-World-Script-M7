do local Players=game:GetService("Players");local RunService=game:GetService("RunService");local TweenService=game:GetService("TweenService");local UserInputService=game:GetService("UserInputService");local VirtualUser=game:GetService("VirtualUser");local player=Players.LocalPlayer;local myCarModel=nil;local KMH_MULT=0.276;local function getVehicleSeat(v) if  not v then return nil;end for _,p in ipairs(v:GetDescendants()) do if p:IsA("VehicleSeat") then return p;end end return nil;end local nitroSpeed=50;local lastKnownCar=nil;local mySeat=nil;local function bindMyCar() local char=player.Character;if  not char then return;end local hum=char:FindFirstChildOfClass("Humanoid");if  not hum then return;end hum:GetPropertyChangedSignal("SeatPart"):Connect(function() local seat=hum.SeatPart;if (seat and seat:IsA("VehicleSeat")) then local model=seat:FindFirstAncestorOfClass("Model");if (model and (model~=workspace)) then myCarModel=model;lastKnownCar=model;mySeat=seat;end else mySeat=nil;myCarModel=nil;lastKnownCar=nil;end end);end player.CharacterAdded:Connect(function() myCarModel=nil;lastKnownCar=nil;mySeat=nil;task.defer(bindMyCar);end);if player.Character then task.defer(bindMyCar);end local trimActive=false;local savedTrimData={totalSeconds=0,totalMoney=0,isRunning=false};local lastAntiAfkTime=tick();local antiAfkConn=nil;local GLOW_SPEED=2;local screenGui=Instance.new("ScreenGui");screenGui.Name="VehicleGUI";screenGui.ResetOnSpawn=false;screenGui.IgnoreGuiInset=true;screenGui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling;local ok=pcall(function() screenGui.Parent=game:GetService("CoreGui");end);if  not ok then screenGui.Parent=player.PlayerGui;end local notifSound=Instance.new("Sound");notifSound.SoundId="rbxassetid://6542152011";notifSound.Volume=1.5;notifSound.Parent=screenGui;local function formatTime(secs) local s=math.floor(secs);local months=math.floor(s/2592000 );local weeks=math.floor((s%2592000)/604800 );local days=math.floor((s%604800)/86400 );local hours=math.floor((s%86400)/3600 );local minutes=math.floor((s%3600)/60 );local seconds=s%60 ;local parts={};if (months>0) then table.insert(parts,months   .. " شهر" );end if (weeks>0) then table.insert(parts,weeks   .. " أسبوع" );end if (days>0) then table.insert(parts,days   .. " يوم" );end if (hours>0) then table.insert(parts,hours   .. " ساعة" );end if (minutes>0) then table.insert(parts,minutes   .. " دقيقة" );end if ((seconds>0) or ( #parts==0)) then table.insert(parts,seconds   .. " ثانية" );end return table.concat(parts," و ");end local function formatMoney(m) if (m==0) then return "0 ريال";end return tostring(math.floor(m))   .. " ريال" ;end local function makeStroke(parent,thick) local st=Instance.new("UIStroke",parent);st.Thickness=thick or 1 ;st.Color=Color3.fromRGB(160,160,160);st.Transparency=0;st.ApplyStrokeMode=Enum.ApplyStrokeMode.Border;return st;end local function applyNotifTextGlow(label,color1,color2) task.spawn(function() while label and label.Parent  do TweenService:Create(label,TweenInfo.new(1.5),{TextColor3=color1}):Play();task.wait(1.5);TweenService:Create(label,TweenInfo.new(1.5),{TextColor3=color2}):Play();task.wait(1.5);end end);end local function applyNotifGoldStroke(object) local st=object:FindFirstChildOfClass("UIStroke") or Instance.new("UIStroke",object) ;st.Thickness=2;st.ApplyStrokeMode=Enum.ApplyStrokeMode.Border;task.spawn(function() while st and st.Parent  do TweenService:Create(st,TweenInfo.new(1.5),{Color=Color3.fromRGB(255,215,0)}):Play();task.wait(1.5);TweenService:Create(st,TweenInfo.new(1.5),{Color=Color3.fromRGB(255,140,0)}):Play();task.wait(1.5);end end);end local function sendNotification(text,duration) local nf=Instance.new("Frame",screenGui);nf.Size=UDim2.new(0.25,0,0.14,0);nf.Position=UDim2.new(1.2,0,0.45,0);nf.BorderSizePixel=0;nf.BackgroundTransparency=1;Instance.new("UICorner",nf);local ng=Instance.new("UIGradient",nf);ng.Color=ColorSequence.new({ColorSequenceKeypoint.new(0,Color3.fromRGB(0,20,50)),ColorSequenceKeypoint.new(1,Color3.fromRGB(0,150,255))});applyNotifGoldStroke(nf);notifSound:Play();TweenService:Create(nf,TweenInfo.new(0.3),{BackgroundTransparency=0}):Play();local msg=Instance.new("TextLabel",nf);msg.Size=UDim2.new(0.82,0,0.4,0);msg.Position=UDim2.new(0.05,0,0.15,0);msg.Text=text;msg.Font=Enum.Font.GothamBold;msg.TextScaled=true;msg.TextColor3=Color3.new(1,1,1);msg.BackgroundTransparency=1;local byl=Instance.new("TextLabel",nf);byl.Size=UDim2.new(1,0,0.2,0);byl.Position=UDim2.new(0,0,0.65,0);byl.Text="By M7";byl.Font=Enum.Font.Michroma;byl.TextScaled=true;byl.BackgroundTransparency=1;applyNotifTextGlow(byl,Color3.fromRGB(255,215,0),Color3.fromRGB(255,140,0));local closeX=Instance.new("TextButton",nf);closeX.Size=UDim2.new(0,11,0,11);closeX.Position=UDim2.new(1, -13,0,3);closeX.BackgroundTransparency=1;closeX.Text="X";closeX.TextColor3=Color3.fromRGB(180,180,180);closeX.TextScaled=true;closeX.Font=Enum.Font.GothamBold;closeX.ZIndex=10;closeX.BorderSizePixel=0;local function dismissNotif() TweenService:Create(nf,TweenInfo.new(0.3,Enum.EasingStyle.Quad),{BackgroundTransparency=1}):Play();nf:TweenPosition(UDim2.new(1.2,0,0.45,0),"In","Quad",0.4,true);task.delay(0.4,function() if (nf and nf.Parent) then nf:Destroy();end end);end closeX.MouseButton1Click:Connect(dismissNotif);nf:TweenPosition(UDim2.new(0.74,0,0.45,0),"Out","Back",0.5,true);task.delay(duration or 5 ,function() if (nf and nf.Parent) then dismissNotif();end end);end local trimInfoShowing=false;local trimInfoFrame=nil;local trimInfoLabel=nil;local trimInfoStroke=nil;local trimInfoGlowVal=0;local trimInfoGlowDir=1;local trimInfoTimeConn=nil;local function destroyTrimInfoPanel() trimInfoShowing=false;if trimInfoTimeConn then trimInfoTimeConn:Disconnect();trimInfoTimeConn=nil;end if (trimInfoFrame and trimInfoFrame.Parent) then TweenService:Create(trimInfoFrame,TweenInfo.new(0.3,Enum.EasingStyle.Quad),{BackgroundTransparency=1}):Play();task.delay(0.35,function() if (trimInfoFrame and trimInfoFrame.Parent) then trimInfoFrame:Destroy();end trimInfoFrame=nil;trimInfoLabel=nil;end);end end local function createTrimInfoPanel() if (trimInfoFrame and trimInfoFrame.Parent) then return;end trimInfoFrame=Instance.new("Frame",screenGui);trimInfoFrame.Size=UDim2.new(0,220,0,115);trimInfoFrame.Position=UDim2.new(1, -230,0,8);trimInfoFrame.BackgroundColor3=Color3.fromRGB(12,12,12);trimInfoFrame.BackgroundTransparency=1;trimInfoFrame.BorderSizePixel=0;trimInfoFrame.ZIndex=50;Instance.new("UICorner",trimInfoFrame).CornerRadius=UDim.new(0,11);trimInfoStroke=makeStroke(trimInfoFrame,1);trimInfoStroke.Color=Color3.fromRGB(160,160,160);local antiAfkLbl=Instance.new("TextLabel",trimInfoFrame);antiAfkLbl.Size=UDim2.new(1, -30,0,14);antiAfkLbl.Position=UDim2.new(0,0,0,3);antiAfkLbl.BackgroundTransparency=1;antiAfkLbl.Text="تم تفعيل وضع anti-afk";antiAfkLbl.TextColor3=Color3.fromRGB(200,200,200);antiAfkLbl.TextScaled=true;antiAfkLbl.Font=Enum.Font.Gotham;antiAfkLbl.TextXAlignment=Enum.TextXAlignment.Center;antiAfkLbl.ZIndex=51;local timeRowFrame=Instance.new("Frame",trimInfoFrame);timeRowFrame.Size=UDim2.new(1, -8,0,22);timeRowFrame.Position=UDim2.new(0,4,0,22);timeRowFrame.BackgroundTransparency=1;timeRowFrame.ZIndex=51;local timeTitleLbl=Instance.new("TextLabel",timeRowFrame);timeTitleLbl.Size=UDim2.new(0,80,1,0);timeTitleLbl.Position=UDim2.new(1, -82,0,0);timeTitleLbl.BackgroundTransparency=1;timeTitleLbl.Text="الوقت المستغرق :";timeTitleLbl.TextColor3=Color3.fromRGB(255,255,255);timeTitleLbl.TextScaled=true;timeTitleLbl.Font=Enum.Font.GothamBold;timeTitleLbl.TextXAlignment=Enum.TextXAlignment.Right;timeTitleLbl.ZIndex=52;local timeValLbl=Instance.new("TextLabel",timeRowFrame);timeValLbl.Size=UDim2.new(0.55,0,1,0);timeValLbl.Position=UDim2.new(0,0,0,0);timeValLbl.BackgroundTransparency=1;timeValLbl.Text=formatTime(savedTrimData.totalSeconds);timeValLbl.TextColor3=Color3.fromRGB(180,180,180);timeValLbl.TextScaled=true;timeValLbl.Font=Enum.Font.RobotoMono;timeValLbl.TextXAlignment=Enum.TextXAlignment.Left;timeValLbl.ZIndex=52;local moneyRowFrame=Instance.new("Frame",trimInfoFrame);moneyRowFrame.Size=UDim2.new(1, -8,0,22);moneyRowFrame.Position=UDim2.new(0,4,0,48);moneyRowFrame.BackgroundTransparency=1;moneyRowFrame.ZIndex=51;local moneyTitleLbl=Instance.new("TextLabel",moneyRowFrame);moneyTitleLbl.Size=UDim2.new(0,90,1,0);moneyTitleLbl.Position=UDim2.new(1, -92,0,0);moneyTitleLbl.BackgroundTransparency=1;moneyTitleLbl.Text="الفلوس المجمعة :";moneyTitleLbl.TextColor3=Color3.fromRGB(255,255,255);moneyTitleLbl.TextScaled=true;moneyTitleLbl.Font=Enum.Font.GothamBold;moneyTitleLbl.TextXAlignment=Enum.TextXAlignment.Right;moneyTitleLbl.ZIndex=52;local moneyValLbl=Instance.new("TextLabel",moneyRowFrame);moneyValLbl.Size=UDim2.new(0.55,0,1,0);moneyValLbl.Position=UDim2.new(0,0,0,0);moneyValLbl.BackgroundTransparency=1;moneyValLbl.Text=formatMoney(savedTrimData.totalMoney);moneyValLbl.TextColor3=Color3.fromRGB(100,255,120);moneyValLbl.TextScaled=true;moneyValLbl.Font=Enum.Font.RobotoMono;moneyValLbl.TextXAlignment=Enum.TextXAlignment.Left;moneyValLbl.ZIndex=52;local RESET_BASE_W=90;local RESET_BASE_H=18;local RESET_BASE_X=UDim2.new(1, -95,1, -26);local resetInInfoF=Instance.new("Frame",trimInfoFrame);resetInInfoF.Size=UDim2.new(0,RESET_BASE_W,0,RESET_BASE_H);resetInInfoF.Position=RESET_BASE_X;resetInInfoF.BackgroundColor3=Color3.fromRGB(255,255,255);resetInInfoF.BackgroundTransparency=0;resetInInfoF.BorderSizePixel=0;resetInInfoF.ZIndex=54;Instance.new("UICorner",resetInInfoF).CornerRadius=UDim.new(0,6);local resetStroke=makeStroke(resetInInfoF,0.7);resetStroke.Color=Color3.fromRGB(160,160,160);local resetInInfoBtn=Instance.new("TextButton",resetInInfoF);resetInInfoBtn.Size=UDim2.new(1,0,1,0);resetInInfoBtn.BackgroundTransparency=1;resetInInfoBtn.Text="إعادة البيانات";resetInInfoBtn.TextColor3=Color3.fromRGB(30,30,30);resetInInfoBtn.TextTransparency=0;resetInInfoBtn.TextScaled=true;resetInInfoBtn.Font=Enum.Font.GothamBold;resetInInfoBtn.ZIndex=55;resetInInfoBtn.MouseEnter:Connect(function() TweenService:Create(resetInInfoF,TweenInfo.new(0.12,Enum.EasingStyle.Back,Enum.EasingDirection.Out),{Size=UDim2.new(0,RESET_BASE_W + 6 ,0,RESET_BASE_H + 4 ),Position=UDim2.new(1, -98,1, -28),BackgroundColor3=Color3.fromRGB(180,180,180)}):Play();end);resetInInfoBtn.MouseLeave:Connect(function() TweenService:Create(resetInInfoF,TweenInfo.new(0.15),{Size=UDim2.new(0,RESET_BASE_W,0,RESET_BASE_H),Position=RESET_BASE_X,BackgroundColor3=Color3.fromRGB(255,255,255)}):Play();end);resetInInfoBtn.MouseButton1Click:Connect(function() notifSound:Play();savedTrimData.totalSeconds=0;savedTrimData.totalMoney=0;lastMoneyValue=nil;local mv=getLeaderstatsMoney();if mv then lastMoneyValue=mv.Value;end if (timeValLbl and timeValLbl.Parent) then timeValLbl.Text="0 ثانية";end if (moneyValLbl and moneyValLbl.Parent) then moneyValLbl.Text="0 ريال";end TweenService:Create(resetInInfoF,TweenInfo.new(0.12,Enum.EasingStyle.Back),{Size=UDim2.new(0,RESET_BASE_W,0,RESET_BASE_H),Position=RESET_BASE_X,BackgroundColor3=Color3.fromRGB(170,170,170)}):Play();task.delay(0.45,function() if (resetInInfoF and resetInInfoF.Parent) then TweenService:Create(resetInInfoF,TweenInfo.new(0.3),{BackgroundColor3=Color3.fromRGB(255,255,255)}):Play();TweenService:Create(resetStroke,TweenInfo.new(0.3),{Color=Color3.fromRGB(160,160,160),Thickness=0.7}):Play();end end);end);local closeInfoBtn=Instance.new("TextButton",trimInfoFrame);closeInfoBtn.Size=UDim2.new(0,18,0,18);closeInfoBtn.Position=UDim2.new(1, -20,0,1);closeInfoBtn.BackgroundTransparency=1;closeInfoBtn.Text="X";closeInfoBtn.TextColor3=Color3.fromRGB(160,160,160);closeInfoBtn.TextScaled=true;closeInfoBtn.Font=Enum.Font.GothamBold;closeInfoBtn.ZIndex=55;closeInfoBtn.BorderSizePixel=0;closeInfoBtn.MouseButton1Click:Connect(destroyTrimInfoPanel);TweenService:Create(trimInfoFrame,TweenInfo.new(0.4,Enum.EasingStyle.Quad),{BackgroundTransparency=0.1}):Play();trimInfoShowing=true;trimInfoTimeConn=RunService.Heartbeat:Connect(function() if  not trimInfoShowing then if trimInfoTimeConn then trimInfoTimeConn:Disconnect();trimInfoTimeConn=nil;end return;end if (trimInfoStroke and trimInfoStroke.Parent) then trimInfoGlowVal=trimInfoGlowVal + (trimInfoGlowDir * (1/60) * GLOW_SPEED) ;if (trimInfoGlowVal>=1) then trimInfoGlowVal=1;trimInfoGlowDir= -1;elseif (trimInfoGlowVal<=0) then trimInfoGlowVal=0;trimInfoGlowDir=1;end local tcv=160 + math.floor(trimInfoGlowVal * 95 ) ;trimInfoStroke.Color=Color3.fromRGB(tcv,tcv,tcv);end end);task.spawn(function() while trimInfoShowing do task.wait(1);if savedTrimData.isRunning then savedTrimData.totalSeconds=savedTrimData.totalSeconds + 1 ;end if (timeValLbl and timeValLbl.Parent) then timeValLbl.Text=formatTime(savedTrimData.totalSeconds);end end end);task.spawn(function() while trimInfoFrame and trimInfoFrame.Parent  do TweenService:Create(moneyValLbl,TweenInfo.new(1.5),{TextColor3=Color3.fromRGB(100,255,120)}):Play();task.wait(1.5);TweenService:Create(moneyValLbl,TweenInfo.new(1.5),{TextColor3=Color3.fromRGB(30,140,50)}):Play();task.wait(1.5);end end);task.spawn(function() while trimInfoFrame and trimInfoFrame.Parent  do TweenService:Create(timeValLbl,TweenInfo.new(1.5),{TextColor3=Color3.fromRGB(200,200,200)}):Play();task.wait(1.5);TweenService:Create(timeValLbl,TweenInfo.new(1.5),{TextColor3=Color3.fromRGB(90,90,90)}):Play();task.wait(1.5);end end);trimInfoLabel=moneyValLbl;end local function getMainBody(v) local main,mx=nil,0;for _,p in ipairs(v:GetDescendants()) do if (p:IsA("BasePart") and (p.Size.Magnitude>mx)) then main=p;mx=p.Size.Magnitude;end end return main;end local function getVehicle() local char=player.Character;if char then local hum=char:FindFirstChildOfClass("Humanoid");if (hum and hum.SeatPart and hum.SeatPart:IsA("VehicleSeat")) then local model=hum.SeatPart:FindFirstAncestorOfClass("Model");if (model and (model~=workspace)) then myCarModel=model;mySeat=hum.SeatPart;return model;end end end return nil;end local lastMoneyValue=nil;local moneyTrackingStarted=false;local function getLeaderstatsMoney() local ls=player:FindFirstChild("leaderstats");if  not ls then return nil;end for _,v in ipairs(ls:GetChildren()) do if (v:IsA("IntValue") or v:IsA("NumberValue")) then if (v.Value and (v.Value>=0)) then return v;end end end return nil;end local function addMoney(amount) if (amount<=0) then return;end if ( not trimActive or  not savedTrimData.isRunning) then return;end savedTrimData.totalMoney=savedTrimData.totalMoney + amount ;if (trimInfoLabel and trimInfoLabel.Parent) then trimInfoLabel.Text=formatMoney(savedTrimData.totalMoney);end end local function startMoneyTracking() if moneyTrackingStarted then return;end moneyTrackingStarted=true;task.spawn(function() task.wait(3);local moneyVal=nil;local moneyConn=nil;local function tryConnect() moneyVal=getLeaderstatsMoney();if moneyVal then lastMoneyValue=moneyVal.Value;if moneyConn then moneyConn:Disconnect();end moneyConn=moneyVal.Changed:Connect(function(newVal) if ( not trimActive or  not savedTrimData.isRunning) then lastMoneyValue=newVal;return;end if (lastMoneyValue and (newVal>lastMoneyValue)) then local diff=newVal-lastMoneyValue ;if ((diff>0) and (diff<=1000000)) then addMoney(diff);end end lastMoneyValue=newVal;end);return true;end return false;end if  not tryConnect() then player.ChildAdded:Connect(function(child) if (child.Name=="leaderstats") then task.wait(0.5);tryConnect();end end);for i=1,30 do task.wait(1);if tryConnect() then break;end end end end);end local function doAntiAfkActions() local char=player.Character;local hrp=char and char:FindFirstChild("HumanoidRootPart") ;local hum=char and char:FindFirstChildOfClass("Humanoid") ;local cam=workspace.CurrentCamera;if hrp then local saved=hrp.CFrame;pcall(function() hrp.CFrame=saved * CFrame.new(0,0.05,0) ;end);task.wait(0.08);pcall(function() hrp.CFrame=saved;end);end task.wait(0.1);local v=getVehicle();local ch=v and getMainBody(v) ;if ch then pcall(function() ch.AssemblyLinearVelocity=ch.AssemblyLinearVelocity + Vector3.new(0,0.01,0) ;end);task.wait(0.05);end task.wait(0.05);if cam then local savedCam=cam.CFrame;pcall(function() cam.CFrame=savedCam * CFrame.Angles(0,math.rad(0.01),0) ;end);task.wait(0.05);pcall(function() cam.CFrame=savedCam;end);end task.wait(0.05);pcall(function() VirtualUser:CaptureController();VirtualUser:ClickButton2(Vector2.new(0,0));end);task.wait(0.05);if (hum and  not getVehicle()) then pcall(function() hum.Jump=true;end);task.wait(0.08);pcall(function() hum.Jump=false;end);end end local function startAntiAfk() if antiAfkConn then antiAfkConn:Disconnect();antiAfkConn=nil;end lastAntiAfkTime=tick();antiAfkConn=RunService.Heartbeat:Connect(function() if  not trimActive then if antiAfkConn then antiAfkConn:Disconnect();antiAfkConn=nil;end return;end if ((tick() -lastAntiAfkTime)>=780) then lastAntiAfkTime=tick();task.spawn(doAntiAfkActions);end end);end local function stopAntiAfk() if antiAfkConn then antiAfkConn:Disconnect();antiAfkConn=nil;end end local TAFREEM_TELEPORT_POS=Vector3.new( -2156.94,4.77, -30994.42);local TAFREEM_FINAL_POS=Vector3.new( -1919.3,4.8, -31091.9);local PARTS_TO_DELETE={{name="Part",pos=Vector3.new( -1905.9,92, -31066.7)},{name="Part",pos=Vector3.new( -1906.8,174.1, -31096.8)},{name="hotel parts",pos=Vector3.new( -2098.1,108.7, -31095.7)},{name="fffrf5",pos=Vector3.new( -2211.3,17.4, -31102.5)},{name="NoSpawnRegion",pos=Vector3.new( -1913.1,111, -31066.4)},{name="hotel parts",pos=Vector3.new( -1906.5,26.4, -31072.3)},{name="hotel parts",pos=Vector3.new( -1907.1,86.2, -31074)},{name="hotel parts",pos=Vector3.new( -1908.6,17.5, -31071)}};local trimTeleportOrigPos=nil;local trimWaitingForCar=false;local trimCarWatchConn=nil;local trimCarDriveBV=nil;local trimCarDriveBP=nil;local trimCarDriveGyro=nil;local trimApproachConn=nil;local trimConn=nil;local trimCleanupFn=nil;local wheelieDir= -1;local TAFREEM_APPROACH_KMH=50;local TAFREEM_CIRCLE_KMH=107;local TAFREEM_APPROACH_STU=(TAFREEM_APPROACH_KMH * 3.63)/3.6 ;local TAFREEM_CIRCLE_STU=(TAFREEM_CIRCLE_KMH * 3.63)/3.6 ;local CIRCLE_RADIUS=50;local function cleanTrimDrive() if (trimApproachConn and trimApproachConn.Connected) then trimApproachConn:Disconnect();end trimApproachConn=nil;if (trimCarDriveBV and trimCarDriveBV.Parent) then trimCarDriveBV:Destroy();end trimCarDriveBV=nil;if (trimCarDriveBP and trimCarDriveBP.Parent) then trimCarDriveBP:Destroy();end trimCarDriveBP=nil;if (trimCarDriveGyro and trimCarDriveGyro.Parent) then trimCarDriveGyro:Destroy();end trimCarDriveGyro=nil;end local function cleanTrimAll() if trimCleanupFn then trimCleanupFn();trimCleanupFn=nil;end if (trimConn and trimConn.Connected) then trimConn:Disconnect();end trimConn=nil;cleanTrimDrive();if (trimCarWatchConn and trimCarWatchConn.Connected) then trimCarWatchConn:Disconnect();end trimCarWatchConn=nil;trimWaitingForCar=false;end local function deleteTargetParts() for _,info in ipairs(PARTS_TO_DELETE) do for _,obj in ipairs(workspace:GetDescendants()) do if ((obj.Name==info.name) and obj:IsA("BasePart")) then if ((obj.Position-info.pos).Magnitude<20) then pcall(function() obj:Destroy();end);break;end end end end end local function startCircleLoop(ch,carModel,charRef) local TRIM_CIRCLE_RADIUS=50;local TRIM_TARGET_STU=(107 * 3.63)/3.6 ;for i=1,3 do pcall(function() ch.AssemblyLinearVelocity=Vector3.zero;ch.AssemblyAngularVelocity=Vector3.zero;end);task.wait(0.05);end local fixedY=ch.Position.Y;local circBV=Instance.new("BodyVelocity");circBV.Name="TrimCircleBV";circBV.MaxForce=Vector3.new(1000000000,0,1000000000);circBV.Velocity=Vector3.zero;circBV.Parent=ch;local circBP=Instance.new("BodyPosition");circBP.Name="TrimCircleBP";circBP.MaxForce=Vector3.new(0,5000000000,0);circBP.P=2000000;circBP.D=15000;circBP.Position=Vector3.new(ch.Position.X,fixedY,ch.Position.Z);circBP.Parent=ch;local circGyro=Instance.new("BodyGyro");circGyro.Name="TrimCircleGyro";circGyro.MaxTorque=Vector3.new(10000000,10000000,10000000);circGyro.P=35000;circGyro.D=1800;circGyro.CFrame=ch.CFrame;circGyro.Parent=ch;local rightVec=ch.CFrame.RightVector;local circleCenter=Vector3.new(ch.Position.X + (rightVec.X * TRIM_CIRCLE_RADIUS * wheelieDir) ,fixedY,ch.Position.Z + (rightVec.Z * TRIM_CIRCLE_RADIUS * wheelieDir) );local toCenter=ch.Position-circleCenter ;local circleAngle=math.atan2(toCenter.Z,toCenter.X);local omega=TRIM_TARGET_STU/TRIM_CIRCLE_RADIUS ;sendNotification("تم بدأ التفريم بنجاح",5);savedTrimData.isRunning=true;local mv=getLeaderstatsMoney();if mv then lastMoneyValue=mv.Value;end createTrimInfoPanel();startAntiAfk();function trimCleanupFn() if (circBV and circBV.Parent) then circBV:Destroy();end if (circBP and circBP.Parent) then circBP:Destroy();end if (circGyro and circGyro.Parent) then circGyro:Destroy();end local seat=getVehicleSeat(carModel);if seat then pcall(function() seat.ThrottleFloat=0;end);pcall(function() seat.SteerFloat=0;end);end end trimConn=RunService.Heartbeat:Connect(function(dt) if ( not ch or  not ch.Parent or  not trimActive) then cleanTrimAll();return;end local seat=getVehicleSeat(carModel);if ( not seat or  not seat.Occupant) then cleanTrimAll();trimActive=false;stopAntiAfk();return;end circleAngle=circleAngle-(omega * dt * wheelieDir) ;local cosA=math.cos(circleAngle);local sinA=math.sin(circleAngle);local tangentDir=Vector3.new(sinA * wheelieDir ,0, -cosA * wheelieDir );if (tangentDir.Magnitude<0.01) then return;end tangentDir=tangentDir.Unit;local idealPos=Vector3.new(circleCenter.X + (cosA * TRIM_CIRCLE_RADIUS) ,fixedY,circleCenter.Z + (sinA * TRIM_CIRCLE_RADIUS) );local drift=idealPos-Vector3.new(ch.Position.X,0,ch.Position.Z) ;if (circBV and circBV.Parent) then circBV.Velocity=(tangentDir * TRIM_TARGET_STU) + (drift * 20) ;end if (circBP and circBP.Parent) then circBP.Position=Vector3.new(ch.Position.X,fixedY,ch.Position.Z);end if (circGyro and circGyro.Parent) then local right=Vector3.new(tangentDir.Z,0, -tangentDir.X);circGyro.CFrame=CFrame.fromMatrix(ch.Position,right,Vector3.new(0,1,0), -tangentDir);end ch.AssemblyLinearVelocity=tangentDir * TRIM_TARGET_STU ;pcall(function() seat.ThrottleFloat=1;end);end);end local function startAnchorThenCircle(ch,carModel) local charRef=player.Character;for i=1,5 do pcall(function() ch.AssemblyLinearVelocity=Vector3.zero;ch.AssemblyAngularVelocity=Vector3.zero;end);task.wait(0.06);end task.wait(0.2);local seatNow=getVehicleSeat(carModel);if seatNow then local char3=player.Character;local hum3=char3 and char3:FindFirstChildOfClass("Humanoid") ;if (hum3 and (hum3.SeatPart~=seatNow)) then pcall(function() seatNow:Sit(hum3);end);task.wait(0.3);end end pcall(function() ch.AssemblyLinearVelocity=Vector3.zero;ch.AssemblyAngularVelocity=Vector3.zero;end);task.wait(0.1);startCircleLoop(ch,carModel,charRef);end local function startCarDriveToFinal(ch,carModel) cleanTrimDrive();local TARGET_POS=TAFREEM_FINAL_POS;local lockedY=TARGET_POS.Y;local APPROACH_STU=TAFREEM_APPROACH_STU;local ACCEL_TIME=3;trimCarDriveBV=Instance.new("BodyVelocity");trimCarDriveBV.Name="TrimDriveBV";trimCarDriveBV.MaxForce=Vector3.new(100000000,0,100000000);trimCarDriveBV.Velocity=Vector3.new(0,0,0);trimCarDriveBV.Parent=ch;trimCarDriveBP=Instance.new("BodyPosition");trimCarDriveBP.Name="TrimDriveBP";trimCarDriveBP.MaxForce=Vector3.new(0,5000000000,0);trimCarDriveBP.P=500000;trimCarDriveBP.D=15000;trimCarDriveBP.Position=Vector3.new(ch.Position.X,lockedY,ch.Position.Z);trimCarDriveBP.Parent=ch;trimCarDriveGyro=Instance.new("BodyGyro");trimCarDriveGyro.Name="TrimDriveGyro";trimCarDriveGyro.MaxTorque=Vector3.new(1000000,1000000,1000000);trimCarDriveGyro.P=25000;trimCarDriveGyro.D=1200;trimCarDriveGyro.CFrame=ch.CFrame;trimCarDriveGyro.Parent=ch;local currentSpeed=0;sendNotification("السيارة تتجه للمكان",5);trimApproachConn=RunService.Heartbeat:Connect(function(dt) if ( not ch or  not ch.Parent or  not trimActive) then cleanTrimDrive();return;end if (trimCarDriveBP and trimCarDriveBP.Parent) then trimCarDriveBP.Position=Vector3.new(ch.Position.X,lockedY,ch.Position.Z);end local toTarget=Vector3.new(TARGET_POS.X-ch.Position.X ,0,TARGET_POS.Z-ch.Position.Z );local dist=toTarget.Magnitude;if (dist<5) then if (trimCarDriveBV and trimCarDriveBV.Parent) then trimCarDriveBV.Velocity=Vector3.new(0,0,0);trimCarDriveBV.MaxForce=Vector3.new(0,0,0);end cleanTrimDrive();local seatStop=getVehicleSeat(carModel);if seatStop then pcall(function() seatStop.ThrottleFloat=0;end);pcall(function() seatStop.SteerFloat=0;end);end task.spawn(function() startAnchorThenCircle(ch,carModel);end);return;end currentSpeed=math.min(currentSpeed + ((APPROACH_STU/ACCEL_TIME) * dt) ,APPROACH_STU);local dir=toTarget.Unit;if (trimCarDriveBV and trimCarDriveBV.Parent) then trimCarDriveBV.Velocity=dir * currentSpeed ;end if (trimCarDriveGyro and trimCarDriveGyro.Parent) then local right=Vector3.new(dir.Z,0, -dir.X);trimCarDriveGyro.CFrame=CFrame.fromMatrix(ch.Position,right,Vector3.new(0,1,0), -dir);end end);end local function waitForCarThenStart() trimWaitingForCar=true;if trimCarWatchConn then trimCarWatchConn:Disconnect();end trimCarWatchConn=RunService.Heartbeat:Connect(function() if  not trimWaitingForCar then if trimCarWatchConn then trimCarWatchConn:Disconnect();trimCarWatchConn=nil;end return;end local v=getVehicle();if (v and myCarModel) then trimWaitingForCar=false;if trimCarWatchConn then trimCarWatchConn:Disconnect();trimCarWatchConn=nil;end task.spawn(function() task.wait(0.5);deleteTargetParts();task.wait(0.3);local ch=getMainBody(v);if ch then startCarDriveToFinal(ch,v);end end);end end);end local mKeyDebounce=false;UserInputService.InputBegan:Connect(function(input,gameProcessed) if gameProcessed then return;end if (input.KeyCode~=Enum.KeyCode.M) then return;end if mKeyDebounce then return;end mKeyDebounce=true;task.delay(0.3,function() mKeyDebounce=false;end);if trimActive then trimActive=false;savedTrimData.isRunning=false;cleanTrimAll();stopAntiAfk();destroyTrimInfoPanel();if getVehicle() then task.spawn(function() task.wait(0.1);local v2=getVehicle();if v2 then local seat2=getVehicleSeat(v2);if seat2 then pcall(function() seat2:Sit(nil);end);end task.wait(0.35);end if trimTeleportOrigPos then local origPos=trimTeleportOrigPos;trimTeleportOrigPos=nil;local char3=player.Character;local hrp3=char3 and char3:FindFirstChild("HumanoidRootPart") ;if hrp3 then hrp3.CFrame=origPos;end end end);elseif trimTeleportOrigPos then local origPos=trimTeleportOrigPos;trimTeleportOrigPos=nil;local char3=player.Character;local hrp3=char3 and char3:FindFirstChild("HumanoidRootPart") ;if hrp3 then hrp3.CFrame=origPos;end end sendNotification("تم إيقاف التفريم",4);return;end if getVehicle() then sendNotification("انزل من السيارة وأعد الضغط على M لبدأ التفريم",5);return;end trimActive=true;local char=player.Character;local hrp=char and char:FindFirstChild("HumanoidRootPart") ;if hrp then trimTeleportOrigPos=hrp.CFrame;end task.spawn(function() local char2=player.Character;local hrp2=char2 and char2:FindFirstChild("HumanoidRootPart") ;if hrp2 then hrp2.CFrame=CFrame.new(TAFREEM_TELEPORT_POS);end task.wait(0.15);sendNotification("رسبن سيارتك وادخلها لبدأ التفريم",6);waitForCarThenStart();end);end);local seatbeltConn=nil;local function startSeatbeltLoop() if seatbeltConn then seatbeltConn:Disconnect();seatbeltConn=nil;end seatbeltConn=RunService.Heartbeat:Connect(function() local char=player.Character;if  not char then return;end local hum=char:FindFirstChildOfClass("Humanoid");if (hum and hum.SeatPart and hum.SeatPart:IsA("VehicleSeat")) then local seat=hum.SeatPart;pcall(function() for _,d in ipairs(seat:GetDescendants()) do if d:IsA("BillboardGui") then local txt=d:FindFirstChildWhichIsA("TextLabel");if (txt and (txt.Text:lower():find("seatbelt") or txt.Text:find("حزام"))) then pcall(function() d.Enabled=false;end);end end end end);end end);end task.spawn(startSeatbeltLoop);player.CharacterAdded:Connect(function() task.wait(1);startSeatbeltLoop();end);task.spawn(startMoneyTracking); end
